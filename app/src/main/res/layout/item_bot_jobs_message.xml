<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="@dimen/general_space"
        android:gravity="start"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/general_space"
            android:background="@drawable/bg_bot_bubble"
            android:maxWidth="@dimen/talk_tile_max_width"
            android:padding="@dimen/talk_tile_padding"
            android:textColor="@android:color/white"
            android:textSize="@dimen/middle_font_size" />

        <TextView
            android:layout_width="@dimen/large_icon_size"
            android:layout_height="@dimen/large_icon_size"
            android:background="@drawable/bg_bot_avatar"
            android:gravity="center"
            android:text="🤖"
            android:textSize="@dimen/talk_tile_icon_text_size"
            android:visibility="gone" />
    </LinearLayout>
    <com.zaozhuang.robot.view.NestedListView
        android:listSelector="@android:color/transparent"
        android:id="@+id/job_list"
        android:layout_width="match_parent"
        android:layout_height="600px"
        android:background="@drawable/radius_white_bg"
        android:paddingLeft="32px"
        android:paddingRight="32px"
        android:scrollbars="vertical"/>
</LinearLayout>